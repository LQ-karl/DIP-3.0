# 核心病种辅助分型测算复核报告

**复核对象**：本地目录测算系统中"核心病种 → 辅助分型（第三步）"全链路
**复核依据**：
- 用户提供的《DIP3.0 辅助分型规则文本》（CCI 四级、触发条件、触发系数、调节系数、恶性 7 亚型 / 非恶性 4 级、年龄分档、ICU 分型）
- 权威数据源 `data/CCI.xlsx`（Charlson 17 项权重字典；**2026-09-14 起以官方附件全量重建，2292 行**）、`data/中重度分型诊断.xlsx`（重度/中度诊断清单；**2026-09-14 起以官方附件替换中/重度，1911 行**）
**复核日期**：2026-07-30（**2026-09-14 补充：官方字典全量替换**）
**涉及代码**：
- `src/core/auxiliary_directory.py`（`CCICalculator` / `DiseaseSeverityClassifier` / `AgeFeatureClassifier` / `ICUStayClassifier` / `AuxiliaryDirectoryCalculator`）
- `src/core/local_directory_generator.py`（`apply_auxiliary_typing` 及其子方法，约 L1705–1932）
- `src/core/auxiliary_directory_exporter.py`（`AuxiliaryDirectoryExporter`，被 `web/app.py` 调用）
- `src/models/models.py`（`MedicalRecord` / `DiseaseGroup`）

---

## 一、总体结论

**框架正确、细节多处偏离规范，且存在"两套互相矛盾的辅助分型实现"。**

- 四维度（CCI / 严重程度 / 年龄 / ICU）的**分型结构、分级顺序、分档边界**整体符合规范；
- 但**调节系数与触发系数是硬编码经验值，完全没有按规范"mj/M 数据驱动"测算**（核心背离）；
- **CCI 计算把主诊断也计入了**（违反"通过其他诊断判断"）；
- **重度/中度诊断范围、CCI 权重字典均为硬编码，与权威 xlsx 漂移/覆盖不全**，应改为运行时加载；
- 本地目录主流程（`apply_auxiliary_typing`）与 Web 导出路径（`AuxiliaryDirectoryExporter`）**使用两套名称、分档、系数完全不同的实现，结论互相打架**。

建议按"重大 → 中度 → 轻微"顺序整改，优先消除数据正确性与一致性问题。

---

## 二、符合规范的部分（确认正确，可保留）

| 项 | 位置 | 说明 |
|---|---|---|
| CCI 四级阈值 | `CCICalculator.get_cci_level` (L136–155) | 极严重≥5 / 严重3-4 / 一般1-2 / 无0，逻辑正确 |
| CCI 累加"同类取最高分" | `calculate_cci` (L91–119) | 符合 Charlson 标准做法 |
| 恶性 7 亚型顺序 | `classify_malignant_tumor` (L169–253) | 死亡→高费用Ⅰ→高费用Ⅱ→转移并发→功能衰竭→器官病损→其他，与规范一致 |
| 非恶性 4 级顺序 | `classify_non_malignant` (L255–295) | 死亡→重度→中度→轻度，与规范一致 |
| 儿科分档边界（calculator 版） | `AgeFeatureClassifier._classify_pediatric` (L355–385) | 0-28天 / 29天-1周岁 / 1-6岁 / 7-17岁，与规范一致 |
| ICU 分档 | `ICUStayClassifier` (L404–451) | 2-7 / 8-14 / 15-30 / 31+天，与规范一致 |
| 触发条件框架 | `apply_auxiliary_typing` (L1705–1776) | 病例数阈值 + 分型病例数阈值 + CV 下降≥20%，且三参数均**已暴露为可配置项**（min_total_cases=15 / min_type_cases=5 / cv_improvement_pct=0.20），符合规范结构 |
| CCI 权重分配 | `CCI_SCORES` (L22–73) | 各病类权重 1/2/3/6 与标准 Charlson（及 CCI.xlsx 的 component 名称）一致 |

---

## 三、偏差清单

### 🔴 重大偏差（必须修正）

#### B1. CCI 把主诊断也计入合并症（违反"其他诊断"原则）
- **规范**："通过病例的**其他诊断**判断所属并发症/合并症类型，将其权重累加"。
- **实现**：`local_directory_generator.py` L1849 `diags = [m['main_diag_code'], m['related_diag_code']]`，主诊断一并进入 `calculate_cci`。
- **影响**：主诊断本身被当合并症计分，CCI 普遍虚高，四级分层失真。
- **整改**：CCI 仅使用其他诊断（次要诊断及附加诊断），剔除主诊断。

#### B2. 调节系数 / 触发系数未数据化，全为硬编码（核心背离）
- **规范**：
  - 触发系数 = 该辅助分型上年度病例平均住院费用 ÷ 该病种上年度平均住院费用；
  - 调节系数 = mj / M（mj=该 j 类辅助分型满足触发条件病例平均住院费用，M=该病种全部病例平均住院费用）；
  - 符合多规则时，纳入触发系数最高的分型。
- **实现**：整套系数（`auxiliary_directory.py` 各处 `coefficient`、各 classifier 返回的 1.0–1.7 固定值）均为**经验硬编码**；"触发系数""mj/M"机制**完全不存在**。`apply_auxiliary_typing` 直接取 `sev['coefficient']` 等作为子组系数。
- **影响**：这是与规范最根本的背离——辅助分型应"基于数据测算调节系数"，当前变成固定加成，无法反映真实费用差异、也无法体现触发系数竞争。
- **整改**：新增测算层——对各辅助子组计算 `mj`（组内平均住院费用）与 `M`（病种全样本平均住院费用），`调节系数 = mj / M`；并按触发系数最高者竞争分流。

#### B3. 权威字典未加载，硬编码与 xlsx 漂移 / 覆盖不全
- **CCI 字典**：`CCI_SCORES` 硬编码 139 个 3 位前缀，与权威 `data/CCI.xlsx`（首轮 135 个前缀）对比：
  - **漏 39 个**（如 J45 哮喘、N03–N05 肾炎、I70 动脉硬化、K55 肠血管、M35 结缔组织细分、G30/G45/G46/G83、B18、C76/C7A、E14、H34、I09/I11/I12/I27/I42/I85/I86、J60–J67/J70、K71/K75、Z49/Z94 等）；
  - **多 44 个**（肿瘤细分 C00–C13、艾滋亚码 B20–B23、痴呆细分 F00/F02、心梗细分 I22–I24、脑血管细分 I66/I68/I69、周围血管细分 I73/I74、J41 等）。
  - 注：xlsx 的 component 含"风湿性疾病/偏瘫或截瘫/肾病"，说明标准 Charlson 17 项本就含这些，代码纳入它们**不算错**；真正问题是**应运行时加载 xlsx 而非维护漂移的硬编码快照**。
  - ✅ **已整改（2026-09-14）**：以官方附件1全量重建 —— 由「4 位前缀码」粒度升级为**逐条 7 位医保版2.0 编码**（2292 条），权重按附件合并单元格**前向填充**后固化（权重 1=797 / 2=1272 / 3=63 / 6=160，**无空值、无 0 分**）。逐条比对：码集合与权重、component 名称**差异均为 0**。
- **重度/中度诊断范围**：`DiseaseSeverityClassifier._has_severe_complication` (L297–313)、`_has_organ_damage` (L315–330) 硬编码极少码（R57/N17/J96/K72/A40–A41、I20–I25/I60–I69/J85–J86/K80–K87/N10–N17）。而权威 `data/中重度分型诊断.xlsx` 中：
  - 重度诊断 = `DIP_ASSISTANT_TYPE=QTZD` 且 `DIP_ASSISTANT_CODE=2`（首轮 **268 条**）；
  - 中度诊断 = `QTZD` 且 `code=1`（首轮 **3053 条**）；
  - 硬编码覆盖量不足字典的零头。
  - ✅ **已整改（2026-09-14）**：按用户裁决「以附件替换中/重度，但**不去掉 `+`/`*` 后缀，完全按照附件的格式**」重建 —— 重度 **221 条**、中度 **1327 条**（合计 1548 条，`DIP_FX_TYPE=ALL`），编码逐字保留（含剑号/星号，如 `A22.801+G01*`）；同时**保留**引擎在用的转移 288 / 放疗 49 / 化疗 26（`DIP_FX_TYPE=ZL`，`code=3/4/5`）不动。逐条比对：缺失 0、分级错 0、名称差异 0。
- **整改**：`CCICalculator` 改为从 `CCI.xlsx` 加载权重（按 component→权重映射）；`_has_severe_complication` / `_has_organ_damage` 改为从 `中重度分型诊断.xlsx` 加载 code=2 / code=1 的诊断码集（QTZD/QTSS）。

#### B4. 两套互相矛盾的辅助分型实现并存
- **路径 A（主流程）**：`apply_auxiliary_typing` 调用 `auxiliary_directory.py` 的 classifier，做真实拆分。
- **路径 B（Web）**：`web/app.py` L358–359 调用 `AuxiliaryDirectoryExporter().classify_records()`，该模块另写一套：
  - CCI 分级名"无合并症/一般/重要/特别重要合并症"，且 `_classify_cci` (L161–167) 把 **score≥4 一律归"特别重要合并症"**（规范 3-4=严重、≥5=极严重，冲突）；
  - 年龄分档"幼儿期1-5岁/学龄期6-11岁"（calculator 是 1-6 / 7-17，与规范"学龄前期1-6、学龄期7-17"对照 calculator 更贴合）；
  - 系数体系与路径 A 完全不同。
- **影响**：同一批数据，CLI 与 Web 给出的辅助分型结论不一致；且 exporter 依赖 `record.cci_score` / `record.severity_level` 等属性，而这些在 CLI 流程中**未被填充**，Web 端实际也读不到正确分型。
- **整改**：统一为单一数据源——保留 `auxiliary_directory.py` 的 classifier 作为唯一引擎，exporter 改为消费其输出（`AuxiliaryClassification` 由 classifier 结果构造），删除 exporter 内自有的矛盾分级。

### 🟡 中度偏差（应修正）

#### B5. 高费用判断基准错误（用了次均费用而非"分值费用标准"）
- **规范**："医疗费用不小于**该病种分值费用标准**3 倍"。
- **实现**：`classify_malignant_tumor` 的 `cost_standard` 参数，在 `AuxiliaryDirectoryCalculator` (L734) 传 `disease_avg_cost * 2`，在 `apply_auxiliary_typing._classify_members` (L1827) 传 `avg_cost`——都是**病种次均费用（或其 2 倍）**，而非 RW×点值（分值费用标准）。
- **整改**：传入真实"分值费用标准"（RW×点值），而非任意平均费用倍数。

#### B6. 未排除中医优势病种 / 床日病种
- **规范**："中医优势病种、床日病种病例不纳入辅助分型范围"。
- **实现**：`apply_auxiliary_typing` 仅排除 `group_type != CORE` 与 `excluded`，**无中医/床日排除逻辑**。
- **整改**：在 `DiseaseGroup` / 入组环节增加 `is_tcm_advantage` / `is_bed_day` 标记，触发前过滤。

#### B7. 多辅助分型规则竞争逻辑缺失
- **规范**："当病例符合多个辅助分型规则时，纳入触发系数最高的分型中计算调节系数"。
- **实现**：`_classify_members` 对每个维度独立分型，最终取 **CV 改善最大者**拆分；无"按触发系数最高竞争分流"（且触发系数本身未实现，见 B2）。
- **整改**：B2 实现后，以此为基础做多规则竞争。

#### B8（B4 子项）. exporter CCI 分级与 calculator 冲突
- 见 B4。`_classify_cci` 的 thresholds（≥4 全归特别重要 + 系数 1.3/1.4）与规范 3-4=严重 / ≥5=极严重、系数体系不符。

#### B9（B4 子项）. exporter 与 calculator 年龄分档矛盾
- 见 B4。exporter `AGE_TYPES` 用 1-5 岁幼儿 / 6-11 学龄期，与规范及 calculator 的 1-6 / 7-17 不一致。

### 🟢 轻微 / 数据依赖（需确认或低优先）

- **B10. ICU 触发未放宽** → **✅已整改（B2/B7 整改时一并实现）**：`AUX_ICU_RELAX_FACTOR=0.5`——ICU 维度的 CV 门槛与 CV 下降门槛均按 0.5 倍放宽（`auxiliary_directory.py` §5.2 闸门）。
- **B11. 65 岁以上未结合严重程度校正**：规范"65 岁以上利用疾病严重程度辅助分型进行校正"。`_classify_elderly` 仅做年龄段分档给系数，未与严重程度结合。—— **✅已整改（2026-09-04 用户裁决"实现结合校正"）**：65+ 年龄子组结合严重程度字典进一步区分，子组标签形如「70-79岁·重度」（`_classify_elderly` 新增 `severity_level` 参数）；三条路径均已接入——①主流程 `apply_auxiliary_typing`/`_classify_members` 传入严重程度等级，重度/中度/轻度分别成桶、分别统计触发系数（mj/M 数据化）；②Web 路径 `calculate_all_auxiliary_coefficients` 复用步骤②算得的严重程度；③评估路径 `_label_age` 同步结合。成人（18-64）与儿科段不受影响。回归：`tests/test_auxiliary.py::test_elderly_severity_correction`。
- **B12. 儿科分档依赖 day_age 字段**：规范"0-28天/29天-1周岁"。代码对 age<1 用 `day_age` 细分，缺失时退化为"年龄<1岁"—— **✅口径已注明**：属数据依赖项，清单缺「天龄/出生日期+入院时间」时无法按规范细分，退化行为合理（不误判成年）。
- **B13. 字段语义核实**：规范"治疗费（不含手术费）占比"。代码用 `treatment_cost`，需确认 `MedicalRecord.treatment_cost` 确为"不含手术费"；同理 `drug_cost` 是否即药品费。—— **⚠️待数据源核实（2026-09-04 用户裁决）**：用户尚未向数据方核实字段定义；已在 `models.py` 两个字段处加「待数据源核实」标注。二者参与严重程度分型高费用判定（治疗费占比/药品费占比≥50%），若实际口径不同（如含手术费）需先修正数据源映射再测算；计算逻辑暂不改动。
- **B14. 上年度病例数代理**：触发条件①"上年度病例数"被 `group.case_count`（当前组病例数）代理。**✅口径已注明**（2026-09-04，`AUX_MIN_CORE_CASES` 处注释）：本地数据无上年度维度，以本年度成组病例数代理；阈值已参数化，地方可得真实上年度数据时应替换口径。

---

## 四、整改优先级路线图

| 优先级 | 偏差 | 工作量 | 说明 |
|---|---|---|---|
| P0 | B1 主诊断混入 CCI | 小 | 改 L1849，仅传其他诊断 |
| P0 | B3 加载 CCI.xlsx / 中重度 xlsx | 中 | 消除硬编码漂移，对齐权威字典 |
| P0 | B4 统一双路径为单引擎 | 中 | 消除结论打架，是其他整改的基础 |
| P1 | B2 实现触发系数 / 调节系数 mj/M | 大 | 核心算法改造，建议先做 B4/B3 |
| P1 | B5 高费用基准改为分值费用标准 | 小 | 传 RW×点值 |
| P1 | B6 排除中医/床日病种 | 小 | 加标记+过滤 |
| P2 | B7 多规则竞争 | 中 | 依赖 B2 |
| P2 | B8/B9 exporter 分级/分档 | 小 | 随 B4 一并消除 |
| P3 | B10/B11/B12/B14 ✅已整改或口径注明；B13 ⚠️待数据源核实 | 小 | 数据质量与口径完善 |

---

## 五、复核方法说明

1. 通读 `auxiliary_directory.py` / `auxiliary_directory_exporter.py` / `apply_auxiliary_typing` 全链路；
2. 用 pandas 解析 `CCI.xlsx`、`中重度分型诊断.xlsx`，程序化比对硬编码字典与权威字典的码覆盖差异（**2026-07-30 首轮**：CCI 漏 39 / 多 44 前缀；重度诊断字典 268 条、中度 3053 条，硬编码仅覆盖数条）；
3. 比对 `web/app.py` 调用关系，确认存在双实现路径。
4. **2026-09-14 补充**：以用户提供的 4 份官方附件为准做全量替换 —— 附件1《Charlson合并症指数CCI字典表.xlsx》（2292 条）重建 `data/CCI.xlsx`；附件3《中重度分型字典表.xlsx》（重度 221 / 中度 1327）替换 `data/中重度分型诊断.xlsx` 的中/重度部分；附件4《综合病种字典表.xlsx》（8192 条）新建 `data/综合病种字典表.xlsx`。重建脚本 `scripts/rebuild_aux_dicts_from_attachments.py`，逐条比对结论见第六节。

> 注：用户规范文本对 Charlson 17 项仅"列举主要项"，未列全（如风湿性/偏瘫/肾病）。经 `CCI.xlsx` 确认这些本属标准 17 项，故代码"包含它们"**不算偏差**；真正问题是硬编码与权威字典的漂移，应改为加载 xlsx。

---

## 六、2026-09-14 官方字典全量替换记录

**触发**：用户提供 4 份官方附件并要求「复核之前的字典表有没有错误或遗漏，如果有错误或遗漏，以附件为准，遍历每个表的所有 sheet」。四附件与项目字典的对应关系及裁决结果：

| 附件 | 内容 | 对应项目字典 | 复核结论 | 用户裁决 |
|---|---|---|---|---|
| 附件1 `Charlson合并症指数CCI字典表.xlsx` | CCI 编码表 2292 条（2 sheet） | `data/CCI.xlsx` | 旧表仅 231 行、4 位前缀粒度、33 条空得分 | **以附件全量重建** |
| 附件2 `按病种分值（DIP）付费3.0版分组方案.xlsx` | 核心病种四层 | `data/DIP3.0国家目录库.xlsx` | **逐字段零差异**（`verify_national_directory.py`） | 无需改动 |
| 附件3 `中重度分型字典表.xlsx` | 重度 221 / 中度 1327 | `data/中重度分型诊断.xlsx` | 旧表粒度/范围与附件差异大 | **替换中/重度，保留 `+`/`*` 后缀，完全按附件格式** |
| 附件4 `综合病种字典表.xlsx` | 2048 类目 × 4 组 = 8192 条 | **无（系统缺失）** | 属功能缺口 | **纳入并接入引擎兜底成组** |

**落地**：
1. **数据**：`scripts/rebuild_aux_dicts_from_attachments.py` 重建 3 张表（旧文件备份至 `output/_dict_backup_20260914/`）。
   - `data/CCI.xlsx`：2292 条，逐条 7 位码 + 前向填充权重；保留 `Sheet1`(ICD10 code / Charlson component / 得分) + `CCI评分说明`。
   - `data/中重度分型诊断.xlsx`：`GX_ASSI` 1911 行 = 附件中/重度 1548 + 原转移 288 / 放疗 49 / 化疗 26；保留"示例和说明" sheet。
   - `data/综合病种字典表.xlsx`：附件4 原样纳入（含 `分组统计` sheet）。
2. **代码**（综合病种兜底层）：
   - `src/core/national_directory_v30.py`：新增 `ZongheMatch`、`_load_zonghe()`（索引 `(ICD-10分类码, 组编号)`）、`zonghe_group_no()`、`zonghe_class_candidates()`、`match_zonghe()`。
   - `src/core/local_directory_generator.py`：`_refine_core_group_key` 在引擎四层未命中后调用 `match_zonghe()`；`_build_group` 以字典的编码/名称成组，`group_type=MIXED`、`grouping_layer="综合病种"`。
3. **回归**：新增 `tests/test_local_directory.py::test_zonghe_dictionary_engine_fallback`；按新行为校准 `test_core_disease_priority_and_merge`（I25.1+36.1200 → `I25-4`）与 `test_diagnostic_auxiliary_subdivision`（Z51+C97/D18 → `Z51-3`；T30.2/T30.100 → `T30-1`；T31.1 → `T31-1`）。**全量 89 项测试通过**。

**保真度核验（逐条比对官方附件，差异均为 0）**：

| 字典 | 条数 | 缺失 | 分级/权重差异 | 名称差异 |
|---|---|---|---|---|
| CCI | 2292 | 0 | 0 | 0 |
| 中重度分型 | 重度 221 / 中度 1327 | 0 | 0 | 0 |
| 综合病种 | 8192 | 0 | 0（8192 行全等） | 0 |

**影响提示**：
- CCI 粒度由 4 位前缀收敛为逐条 7 位码，并新增大量拓展码（`X` 码）——同一病种不同术式/部位的 CCI 判定将更精细，可能改变部分病种的分型结果。
- 中重度诊断范围变化直接改变严重程度分型（重度/中度/轻度/高费用型）的判定基数。
- 综合病种兜底改变了「引擎未命中 → 合成 `diag4|op|` 键」的旧行为：现在优先按官方字典入组，形成 `类目码-组编号` 键；该行为已由用户裁决确认。

**新旧 CCI 字典覆盖对比（供确认，⚠️ 需用户知悉）**：

| 指标 | 旧表 | 新表（附件1） | 说明 |
|---|---|---|---|
| 逐条码 | 231 | 2292 | — |
| 3 位前缀 | 115 | 133 | +20 新增 / **-2 旧有新无** |
| 4 位前缀 | 121 | 531 | +415 新增 / **-5 旧有新无** |
| 同前缀权重变化 | — | — | 仅 `Z94`：旧 1？→新 2（见下，实为**修复**） |

- ✅ **修复项**：旧表 `Z94.0 → 肾病` 的「得分」为**空**（`_load_cci` 会跳过，即无权重）；新表 `Z94.000/001/002 → 中重度肾脏疾病 = 2`，覆盖更完整。旧表另有 33 条空得分记录属同类问题，重建后全部消除。
- ⚠️ **旧表有、附件1 未覆盖的 8 条**（按「以附件为准」未回填，**如临床需要请指示是否补回**）：

| 旧码 | 旧 component | 旧权重 | 说明 |
|---|---|---|---|
| `C7A.0` | 转移性实体瘤 | 6 | ICD-10 新增类目（神经内分泌肿瘤），附件未收录 |
| `G46` | 脑血管疾病 | 1 | ICD-10 星号（`*`）类目，附件未收录 |
| `C85.8` | 任何恶性肿瘤 | 2 | ICD-10 中 C85 无 .8 亚目，疑旧表笔误 |
| `C90.21` | 任何恶性肿瘤 | 2 | 医保版2.0 拓展码，附件未收录 |
| `I27.81` / `I27.82` | 慢性肺病 | 1 | 医保版2.0 拓展码，附件未收录（附件 I27 仅 `I27.900` 等） |
| `I67.83` / `I67.84` | 脑血管疾病 | 1 | 医保版2.0 拓展码，附件未收录（附件 I67 共 35 条，不含 .8x） |

> 这 8 条来自旧硬编码表（B3 已指出其存在漂移与覆盖不全），合法性待核。**当前实现严格按附件1，未回填**；如需回填请告知。
